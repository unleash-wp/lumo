# Dependencies
node_modules/

# Build output
dist/
dist-skills/
build/

# Secrets / env
.env
.env.*
!.env.example

# Claude Code local-only settings
.claude/settings.local.json
.claude/hooks/.logs/

# Agent tooling cache (never ship)
.impeccable/
.agent-transcripts/
.cursor/projects/

# OS / editor
.DS_Store
*.log

# npm pack output (build artifact)
*.tgz

# Local smoke fixtures
.tmp-check/
.tmp-prodev-bait/

# Local Claude Code / assistant tooling. Vendored third-party skill packs live
# here under their own licences (Apache-2.0, GPL-3.0, source-available); this
# repository is MIT and must not appear to relicense them.
#
# Three files here are ours and ship as a product surface: the PreToolUse
# enforcement hook (tsup builds dist/hook-catch.mjs for it, see tsup.config.ts),
# its tests, and the settings.json that wires it. Those stay tracked.
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/hooks/*
!.claude/hooks/wp-enforce.cjs
!.claude/hooks/__tests__/
.claude/hooks/__tests__/*
!.claude/hooks/__tests__/wp-enforce.test.cjs
CLAUDE.md

# .mcp.json is TRACKED on purpose. It was untracked in 8bca2ff as an
# "internal-only file", on the reasoning that it wires any checkout of this
# repo to the paid hosted MCP. That reasoning was correct then and is wrong
# now: .claude-plugin/marketplace.json declares "source": "./", so this
# repository IS the plugin, and .mcp.json is the file Claude Code reads to
# find its server. Ignored, the plugin installs with no server at all --
# which is the one thing it exists to do.

# Internal planning scaffold; the real docs are archived outside the repo.
plans/
