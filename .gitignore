# Dependencies
node_modules/

# Build output
dist/
dist-skills/
build/

# Secrets / env
.env
.env.*
!.env.example

# Claude Code local-only settings
.claude/settings.local.json
.claude/hooks/.logs/

# Agent tooling cache (never ship)
.impeccable/
.agent-transcripts/
.cursor/projects/

# OS / editor
.DS_Store
*.log

# npm pack output (build artifact)
*.tgz

# Local smoke fixtures
.tmp-check/
.tmp-prodev-bait/

# Local Claude Code / assistant tooling. Vendored third-party skill packs live
# here under their own licences (Apache-2.0, GPL-3.0, source-available); this
# repository is MIT and must not appear to relicense them.
.claude/
CLAUDE.md

# Wires any checkout of this repo to the paid hosted MCP. Not a project default.
.mcp.json
