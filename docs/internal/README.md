# Internal founder notes (not shipped)

Markdown here is for UnleashWP planning, dogfood, pricing experiments, and
go-live checklists. It is **not** included in the `@unleashwp/lumo` npm package
(`package.json` `files` allowlist excludes `docs/`), and it must never appear in
public release artefacts.

Do not move customer-facing install or product docs here. Those stay in `docs/`
at the repo root.
